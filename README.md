# 📊 تحليل الالتواء (Skewness) باستخدام Python

## 📌 نظرة عامة

يهدف هذا المشروع إلى توضيح مفهوم **الالتواء (Skewness)** وتأثيره في شكل توزيع البيانات باستخدام لغة البرمجة **Python**.

يقوم البرنامج بإنشاء ومقارنة ثلاثة أنواع من التوزيعات:

1. **التوزيع الطبيعي (Normal Distribution)**
2. **التوزيع المنحرف إلى اليمين (Right-Skewed Distribution)**
3. **التوزيع المنحرف إلى اليسار (Left-Skewed Distribution)**

لكل توزيع، يقوم البرنامج بحساب وعرض:

* المتوسط الحسابي (Mean)
* الوسيط (Median)
* الانحراف المعياري (Standard Deviation)
* الالتواء (Skewness)
* المدرج التكراري (Histogram)
* منحنى الكثافة (Kernel Density Estimate – KDE)

الهدف من المشروع هو تقديم طريقة عملية وبصرية لفهم **شكل توزيع البيانات** وكيف يمكن أن يؤثر الالتواء في العلاقة بين المتوسط والوسيط.

---

# 🎯 هدف المشروع

يقوم العديد من الباحثين بعد جمع البيانات مباشرة بحساب:

* المتوسط
* الانحراف المعياري
* معاملات الارتباط
* الاختبارات الإحصائية

ولكن قبل الانتقال إلى التحليل الإحصائي، من المهم أولًا فهم **شكل البيانات وتوزيعها**.

يوضح هذا المشروع بصورة بصرية الفرق بين التوزيع المتماثل والتوزيعات غير المتماثلة، ويساعد على فهم العلاقة بين:

**Mean → Median → Skewness**

---

# 📚 ما هو الالتواء (Skewness)؟

الالتواء هو مقياس إحصائي يصف **درجة عدم تماثل توزيع البيانات حول مركزها**.

بصورة عامة يمكن أن نجد ثلاث حالات رئيسية:

| نوع التوزيع             | قيمة Skewness | شكل التوزيع                   |
| ----------------------- | ------------: | ----------------------------- |
| التوزيع الطبيعي تقريبًا |           ≈ 0 | متماثل تقريبًا                |
| الالتواء الموجب         |           > 0 | ذيل أطول باتجاه القيم الكبيرة |
| الالتواء السالب         |           < 0 | ذيل أطول باتجاه القيم الصغيرة |

> **ملاحظة:** قيمة الالتواء القريبة من الصفر تعني أن التوزيع قريب من التماثل، لكنها لا تكفي وحدها لإثبات أن البيانات تتبع التوزيع الطبيعي.

---

# 1️⃣ التوزيع الطبيعي (Normal Distribution)

يتميز التوزيع الطبيعي تقريبًا بالتماثل حول مركز التوزيع.

في الحالة المثالية يكون:

```text
Mean ≈ Median
```

كما تكون قيمة الالتواء قريبة من:

```text
Skewness ≈ 0
```

---

# 2️⃣ التوزيع المنحرف إلى اليمين

في التوزيع المنحرف إلى اليمين، يكون هناك **ذيل أطول باتجاه القيم الكبيرة**.

وغالبًا نجد:

```text
Mean > Median
```

وتكون قيمة:

```text
Skewness > 0
```

أي أن الالتواء يكون موجبًا.

---

# 3️⃣ التوزيع المنحرف إلى اليسار

في التوزيع المنحرف إلى اليسار، يكون هناك **ذيل أطول باتجاه القيم الصغيرة**.

وغالبًا نجد:

```text
Mean < Median
```

وتكون قيمة:

```text
Skewness < 0
```

أي أن الالتواء يكون سالبًا.

---

# 🧰 المكتبات المستخدمة

يستخدم المشروع ثلاث مكتبات رئيسية في Python:

## NumPy

تستخدم من أجل:

* توليد البيانات العشوائية
* حساب المتوسط
* حساب الوسيط
* حساب الانحراف المعياري
* إنشاء النطاقات العددية

```python
import numpy as np
```

---

## Matplotlib

تستخدم من أجل:

* إنشاء Histogram
* رسم منحنى KDE
* إضافة خطوط المتوسط والوسيط
* إنشاء الشكل النهائي
* حفظ الصورة

```python
import matplotlib.pyplot as plt
```

---

## SciPy

يستخدم المشروع من مكتبة SciPy الدالتين:

```python
from scipy.stats import skew, gaussian_kde
```

حيث تستخدم:

```python
skew()
```

لحساب معامل الالتواء.

بينما تستخدم:

```python
gaussian_kde()
```

لحساب وتقدير منحنى الكثافة الاحتمالية.

---

# 📦 تثبيت المكتبات

تأكد أولًا من تثبيت Python.

بعد ذلك يمكن تثبيت المكتبات المطلوبة باستخدام:

```bash
pip install numpy matplotlib scipy
```

أو يمكن إنشاء ملف:

```text
requirements.txt
```

يحتوي على:

```text
numpy
matplotlib
scipy
```

ثم تنفيذ:

```bash
pip install -r requirements.txt
```

---

# 🚀 تشغيل البرنامج

احفظ الكود باسم:

```text
skewness_analysis.py
```

ثم شغله باستخدام:

```bash
python skewness_analysis.py
```

سيقوم البرنامج تلقائيًا بـ:

1. إنشاء ثلاثة أنواع من البيانات.
2. حساب الإحصاءات الوصفية.
3. إنشاء ثلاثة مخططات.
4. رسم Histogram.
5. رسم منحنى KDE.
6. إضافة المتوسط والوسيط.
7. حساب Skewness.
8. حفظ الصورة النهائية.

وسيتم حفظ الشكل باسم:

```text
skewness_comparison.png
```

---

# 🔢 توليد البيانات

يتم إنشاء:

```python
n = 1000
```

أي أن كل توزيع يحتوي على **1000 مشاهدة**.

ويتم تثبيت الرقم العشوائي باستخدام:

```python
np.random.seed(42)
```

والهدف من ذلك هو ضمان إمكانية **إعادة إنتاج النتائج (Reproducibility)** عند تشغيل البرنامج مرة أخرى.

---

# 1. إنشاء التوزيع الطبيعي

يتم إنشاء البيانات باستخدام:

```python
normal_data = np.random.normal(
    loc=50,
    scale=10,
    size=n
)
```

حيث:

* `loc = 50` يمثل المتوسط.
* `scale = 10` يمثل الانحراف المعياري.
* `size = 1000` يمثل عدد المشاهدات.

---

# 2. إنشاء التوزيع المنحرف إلى اليمين

يستخدم البرنامج توزيع Gamma:

```python
right_skewed_data = np.random.gamma(
    shape=2.0,
    scale=8.0,
    size=n
) + 30
```

ويتم استخدام توزيع Gamma لأنه يسمح بإنشاء بيانات ذات **التواء موجب**.

ثم تتم إضافة:

```python
+ 30
```

لإزاحة البيانات باتجاه قيم أكبر.

---

# 3. إنشاء التوزيع المنحرف إلى اليسار

يتم إنشاء التوزيع المنحرف إلى اليسار من خلال عكس التوزيع المنحرف إلى اليمين:

```python
left_skewed_data = 110 - right_skewed_data
```

تؤدي هذه العملية إلى عكس اتجاه الالتواء.

وبالتالي يتحول:

```text
Positive Skewness
```

إلى:

```text
Negative Skewness
```

---

# 📊 الإحصاءات المستخدمة

يقوم البرنامج بحساب أربعة مقاييس أساسية.

## المتوسط الحسابي (Mean)

```python
mean_value = np.mean(data)
```

يمثل المتوسط الحسابي مجموع القيم مقسومًا على عدد المشاهدات.

---

## الوسيط (Median)

```python
median_value = np.median(data)
```

يمثل القيمة التي تقع في منتصف البيانات بعد ترتيبها.

---

## الانحراف المعياري (Standard Deviation)

يتم حساب الانحراف المعياري للعينة باستخدام:

```python
np.std(data, ddof=1)
```

استخدام:

```python
ddof=1
```

يعني حساب **الانحراف المعياري للعينة (Sample Standard Deviation)**.

---

## الالتواء (Skewness)

يتم حساب الالتواء باستخدام:

```python
skewness_value = skew(data)
```

وهو مقياس لمدى عدم تماثل توزيع البيانات.

---

# 📈 المدرج التكراري (Histogram)

يستخدم البرنامج Histogram لعرض شكل توزيع البيانات:

```python
ax.hist(
    data,
    bins=30,
    density=True,
    alpha=0.65,
    edgecolor="black"
)
```

يتم تقسيم البيانات إلى:

```text
30 bins
```

ويتم استخدام:

```python
density=True
```

بحيث يمثل المدرج **كثافة الاحتمال** بدلًا من التكرار الخام.

---

# 📉 منحنى الكثافة KDE

إضافة إلى Histogram، يستخدم البرنامج:

**Kernel Density Estimate (KDE)**

لتكوين منحنى أكثر نعومة يمثل شكل التوزيع.

```python
kde = gaussian_kde(data)
```

ثم يتم إنشاء مجموعة من القيم:

```python
x = np.linspace(
    data.min(),
    data.max(),
    400
)
```

ورسم منحنى الكثافة:

```python
ax.plot(
    x,
    kde(x),
    linewidth=2
)
```

وبذلك يمكن مقارنة الشكل الخام للبيانات مع منحنى كثافة أكثر نعومة.

---

# 📍 المتوسط والوسيط على الرسم

يقوم البرنامج بإضافة خط رأسي يمثل المتوسط:

```python
ax.axvline(
    mean_value,
    linestyle="--",
    linewidth=2
)
```

وخط آخر يمثل الوسيط:

```python
ax.axvline(
    median_value,
    linestyle=":",
    linewidth=2
)
```

وهذا يجعل العلاقة بين المتوسط والوسيط واضحة بصريًا.

مثلًا في التوزيع المنحرف إلى اليمين:

```text
Median < Mean
```

وفي التوزيع المنحرف إلى اليسار:

```text
Mean < Median
```

---

# 🔬 تفسير التوزيعات الثلاثة

## التوزيع الطبيعي

يكون التوزيع متماثلًا تقريبًا:

```text
Mean ≈ Median
```

وقيمة Skewness تكون قريبة من الصفر.

---

## التوزيع المنحرف إلى اليمين

وجود ذيل أطول باتجاه القيم الكبيرة يؤدي عادةً إلى سحب المتوسط باتجاه اليمين:

```text
Median < Mean
```

وتكون قيمة:

```text
Skewness > 0
```

---

## التوزيع المنحرف إلى اليسار

وجود ذيل أطول باتجاه القيم الصغيرة يؤدي عادةً إلى سحب المتوسط باتجاه اليسار:

```text
Mean < Median
```

وتكون قيمة:

```text
Skewness < 0
```

---

# 🖼️ الشكل الناتج

يقوم البرنامج بإنشاء ثلاثة مخططات جنبًا إلى جنب:

```text
┌────────────────┬────────────────────┬─────────────────┐
│ Normal         │ Right-Skewed       │ Left-Skewed     │
│ Distribution   │ Distribution       │ Distribution    │
│                │                    │                 │
│ Skew ≈ 0       │ Skew > 0           │ Skew < 0        │
└────────────────┴────────────────────┴─────────────────┘
```

ويتم حفظ الشكل بدقة عالية:

```python
plt.savefig(
    "skewness_comparison.png",
    dpi=300,
    bbox_inches="tight"
)
```

استخدام:

```text
dpi=300
```

يجعل الصورة مناسبة للاستخدام في التقارير والمحاضرات والمحتوى التعليمي.

---

# 🖨️ النتائج المطبوعة

بعد إنشاء الرسوم، يقوم البرنامج بطباعة النتائج لكل توزيع:

```text
Normal Distribution
----------------------------------------
Mean: ...
Median: ...
Standard Deviation: ...
Skewness: ...

Right-Skewed Distribution
----------------------------------------
Mean: ...
Median: ...
Standard Deviation: ...
Skewness: ...

Left-Skewed Distribution
----------------------------------------
Mean: ...
Median: ...
Standard Deviation: ...
Skewness: ...
```

وهذا يسمح بمقارنة النتائج الرقمية مع الشكل البصري للتوزيع.

---

# 🌾 مثال تطبيقي في البحوث الزراعية

يمكن تطبيق مفهوم الالتواء على بيانات زراعية حقيقية.

على سبيل المثال، لنفترض أن الباحث قام بقياس **إنتاجية محصول القمح (طن/هكتار)** في عدد كبير من الحقول.

قد تكون معظم القيم بين:

```text
4 – 6 ton/ha
```

ولكن توجد بعض الحقول ذات إنتاجية مرتفعة جدًا.

قد يؤدي وجود هذه القيم العالية إلى ظهور **التواء موجب** في البيانات.

في هذه الحالة، لا يكون من الأفضل النظر إلى:

```text
Mean
```

بمعزل عن بقية المعلومات.

بل يمكن للباحث فحص:

* Histogram
* Mean
* Median
* Standard Deviation
* Skewness

من أجل تكوين صورة أكثر شمولًا عن توزيع البيانات.

---

# 🧠 لماذا يجب أن نهتم بشكل التوزيع؟

يمكن تمثيل سير العمل الإحصائي بصورة مبسطة:

```text
جمع البيانات
     ↓
تنظيف البيانات
     ↓
التحليل الاستكشافي للبيانات
     ↓
فحص شكل التوزيع
     ↓
الإحصاءات الوصفية
     ↓
الاختبارات الإحصائية
     ↓
تفسير النتائج
```

لذلك فإن فهم شكل البيانات يمثل جزءًا مهمًا من:

**Exploratory Data Analysis (EDA)**

ولا ينبغي أن يكون التحليل الإحصائي مجرد حساب للمتوسط والانحراف المعياري دون النظر إلى البيانات نفسها.

---

# 📁 هيكل المشروع المقترح

يمكن تنظيم مستودع GitHub بالشكل التالي:

```text
skewness-analysis/
│
├── skewness_analysis.py
├── skewness_comparison.png
├── requirements.txt
└── README.md
```

---

# 💻 الكود الكامل

```python
import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import skew, gaussian_kde

# تثبيت النتائج
np.random.seed(42)

n = 1000

# 1. التوزيع الطبيعي
normal_data = np.random.normal(
    loc=50,
    scale=10,
    size=n
)

# 2. التوزيع المنحرف جهة اليمين
right_skewed_data = np.random.gamma(
    shape=2.0,
    scale=8.0,
    size=n
) + 30

# 3. التوزيع المنحرف جهة اليسار
left_skewed_data = 110 - right_skewed_data

datasets = [
    ("Normal Distribution", normal_data),
    ("Right-Skewed Distribution", right_skewed_data),
    ("Left-Skewed Distribution", left_skewed_data)
]

# إنشاء الشكل
fig, axes = plt.subplots(
    1, 3,
    figsize=(18, 5)
)

fig.suptitle(
    "Effect of Skewness on Data Distribution",
    fontsize=18,
    fontweight="bold"
)

# رسم التوزيعات
for ax, (title, data) in zip(axes, datasets):

    # حساب الإحصاءات
    mean_value = np.mean(data)
    median_value = np.median(data)
    skewness_value = skew(data)

    # Histogram
    ax.hist(
        data,
        bins=30,
        density=True,
        alpha=0.65,
        edgecolor="black"
    )

    # منحنى الكثافة KDE
    kde = gaussian_kde(data)

    x = np.linspace(
        data.min(),
        data.max(),
        400
    )

    ax.plot(
        x,
        kde(x),
        linewidth=2
    )

    # خط المتوسط
    ax.axvline(
        mean_value,
        linestyle="--",
        linewidth=2,
        label=f"Mean = {mean_value:.2f}"
    )

    # خط الوسيط
    ax.axvline(
        median_value,
        linestyle=":",
        linewidth=2,
        label=f"Median = {median_value:.2f}"
    )

    # العنوان
    ax.set_title(
        f"{title}\n"
        f"Skewness = {skewness_value:.2f}",
        fontsize=12,
        fontweight="bold"
    )

    ax.set_xlabel("Values")
    ax.set_ylabel("Density")

    ax.grid(
        axis="y",
        alpha=0.25
    )

    ax.legend(
        fontsize=9
    )

# تنسيق الشكل
plt.tight_layout(
    rect=[0, 0, 1, 0.92]
)

# حفظ الصورة بدقة عالية
plt.savefig(
    "skewness_comparison.png",
    dpi=300,
    bbox_inches="tight"
)

plt.show()

# طباعة النتائج
for title, data in datasets:

    print("\n" + title)
    print("-" * 40)

    print(f"Mean: {np.mean(data):.3f}")

    print(f"Median: {np.median(data):.3f}")

    print(
        f"Standard Deviation: "
        f"{np.std(data, ddof=1):.3f}"
    )

    print(
        f"Skewness: "
        f"{skew(data):.3f}"
    )
```

---

# 📌 الخلاصة

يوضح هذا المشروع أن:

* **Skewness** يقيس عدم تماثل توزيع البيانات.
* التوزيع الطبيعي يكون متماثلًا تقريبًا.
* الالتواء الموجب يعني وجود ذيل أطول باتجاه القيم الكبيرة.
* الالتواء السالب يعني وجود ذيل أطول باتجاه القيم الصغيرة.
* المتوسط يتأثر بالقيم المتطرفة أكثر من الوسيط في كثير من الحالات.
* مقارنة **Mean و Median** تساعد على فهم اتجاه الالتواء.
* الرسم البياني يوفر معلومات مهمة لا تظهر من خلال الأرقام وحدها.
* فحص توزيع البيانات يمثل خطوة مهمة ضمن **التحليل الاستكشافي للبيانات (EDA)**.

---

# 👨‍🔬 الفئة المستهدفة

هذا المشروع مناسب بشكل خاص لـ:

* الباحثين الزراعيين
* الباحثين في علوم البيئة
* الباحثين في التربة والمياه
* طلبة الماجستير والدكتوراه
* طلبة الإحصاء وتحليل البيانات
* الباحثين الذين يتعلمون Python
* الباحثين الذين يتعلمون Exploratory Data Analysis

---

# 👤 المؤلف

**د. عبدالكريم سحاب الدبسا**

باحث في العلوم الزراعية والبيئية
إدارة الأراضي والموارد المائية | التحسس النائي | Python | R | التحليل الإحصائي

---

# 📄 الترخيص

يمكن استخدام هذا المشروع وتعديله لأغراض تعليمية وبحثية، مع الإشارة إلى المؤلف الأصلي عند إعادة استخدام الكود أو تطويره.
